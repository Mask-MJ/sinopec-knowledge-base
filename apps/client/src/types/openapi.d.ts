/**
 * OpenAPI 生成的类型定义
 *
 * 运行 `pnpm openapi` 生成
 * 配置见 redocly.yaml
 */
export interface paths {
  // OpenAPI 类型将在运行 openapi-typescript 后自动生成
  [key: string]: unknown;
}

export interface components {
  schemas: Record<string, unknown>;
}
