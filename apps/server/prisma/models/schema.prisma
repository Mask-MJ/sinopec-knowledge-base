generator client {
  provider        = "prisma-client"
  output          = "../../src/prisma/generated"
  engineType      = "client"
  moduleFormat    = "cjs"
  previewFeatures = ["typedSql"]
}

datasource db {
  provider = "postgresql"
}
