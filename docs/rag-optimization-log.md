# RAG 调优台账

> 每做一次调优就在这里追加一条。目的是**让"这个数字是怎么来的"永远可追溯**，不必再从 commit、聊天记录或某个人的记忆里反推。
>
> 与 [kb-optimization-report.md](kb-optimization-report.md) 的分工：那份是阶段性**汇报**（叙事型，截至 2026-05-09，面向对外说明"我们做了什么、效果如何"）；这份是**流水台账**，一次实验一条，持续追加。历史结论仍以汇报文档为准，本文件只收录 2026-08 之后、有明确单变量对照数据的条目。

## 为什么必须记口径

同一套系统在不同尺子下能给出天差地别的数字。已经踩过的坑：宋工的严口径、我们 xlsx 的宽口径（90 分制）、不同跑批轮次三者混用，导致"优良率"数字对不上（详见 [rag-eval-cross-standard-0522-vs-0607.md](rag-eval-cross-standard-0522-vs-0607.md)）。

**所以每条记录都必须写清"哪把尺子"**：题集 + 题数 + 打分方式 + 助手 + 知识库 + 切片配置 + 模型。缺任何一项，这条记录以后就不可比，等于白记。

尤其注意两套分数不能混：

- **规则分**：答案里必须出现指定数字才算命中（`mustContain` / `mustNotContain`）。脚本自算，不需要外部模型，稳定可复现。
- **LLM judge 分**：另建一个助手当裁判，对开放题打 0~1 分，默认重复 3 次取均值。需要 `RAGFLOW_JUDGE_ASSISTANT_ID`。

**规则分和 judge 分不能直接比大小**，也不能跨题集比。

## 记录格式

    ## YYYY-MM-DD 一句话说清改了什么

    **动机**：为什么要改（触发的现象 / 问题）
    **变量**：改了哪个参数，从什么到什么。是否单变量。
    **口径**：题集 / 题数 / 打分方式 / 助手 / 知识库 / 切片 / chat 模型 / embedding / rerank
    **结果**：对照数据表
    **结论**：采纳与否，为什么
    **落地**：代码 SSOT 位置 + commit + 线上是否同步

---

## 2026-09-01 关闭 frequency_penalty（0.7 → 0）

**动机**：线上聊天出现回答尾部退化成乱码——引用先烂成空的 `[]`，接着雪崩成 `suite_suite__text__` / `XNUMX` / 大段 `x` 和标点，一路刷到长度上限。2026-08-30、09-01 各出现一次。

**变量**：`llm_setting.frequency_penalty` `0.7 → 0`。单变量，其余参数（`temperature=0.1` / `top_p=0.3` / `presence_penalty=0.4`）不动。

原值 0.7 是 RAGFlow 出厂"精确"档，我们建库时照抄了。它是**累加式**惩罚（`logit -= fp × 出现次数`），而长答案里 `[ID:n]` 引用和中文标点会重复几十次，被逐个罚出 `top_p=0.3` 的候选窗口后，模型只能从长尾捡 token，进入不可逆的雪崩。

**口径**：

| 项 | 值 |
| --- | --- |
| 题集 | `questions-0820.json`，32 题（dev 21 + holdout 11） |
| 打分 | 规则分 19 题参与对比；12 道 judge 题**未打分**（线上无 judge 助手） |
| 助手 | 聊天助手-0820-512-topN30（`top_n=30` / `top_k=1024` / `thr=0.2` / `vsw=0.7`） |
| 知识库 | 测试知识库-0820-512（`chunk_token_num=512` / DeepDOC / delimiter `\n`） |
| chat | `qwen3.8-max@aliyun-maas`（阿里云百炼，OpenAI 兼容） |
| embedding | `qwen3.7-text-embedding@aliyun-maas` |
| rerank | `BAAI/bge-reranker-v2-m3@siliconflow` |

**结果**：

先做单变量重放（同一助手、同一问题，只改本参数）：

|                   | fp=0.7             | fp=0      |
| ----------------- | ------------------ | --------- |
| 答案长度          | 1639 字            | 2171 字   |
| 完整 `[ID:n]`     | 22 个              | **47 个** |
| 被烧成空壳的 `[]` | **16 个**          | **0 个**  |
| 标点              | 后半段顿号句号消失 | 正常      |

再做 32 题 A/B（对照组用临时助手 `ab-fp07-temp`，跑完即删，不碰线上）：

|                     | fp=0                  | fp=0.7                |
| ------------------- | --------------------- | --------------------- |
| MRR / hit@1 / hit@3 | 0.938 / 93.8% / 93.8% | 0.938 / 93.8% / 93.8% |
| 规则分均值（19 题） | 0.873                 | 0.889                 |
| 最长答案            | 1484 字               | **43692 字**          |
| 跑挂的题            | **0**                 | **2**                 |

- 检索指标完全一致 —— 符合预期，该参数只影响生成。这也反证两轮跑批本身可比。
- 规则分低 0.016，差异只来自三题：Q4 `+0.22`（fp=0 更好）、Q12 `-0.07`、Q26 `-0.45`。**Q26 是打分假阴性**：两组答案的数字一个不差，只是 fp=0 那次用了表格排版（`| 24 |`），而规则要求数字紧跟单位（`24个`）才算命中。剔除 Q26 后 fp=0 反而略高（`+0.009`）。
- fp=0.7 组当场复现两次故障：**Q11** 刷到 43692 字（前 1300 字正常 → 泄漏英文思考独白 → 退化成同义词接龙 `extradite repatriate return restore reinstate…`，因为每个词用过就被罚，只能不停换新词）；**Q2** 跑了 5 分钟后 `fetch failed`，没拿到答案。
- 注意 **Q11 的规则分是满分 1.00** —— 前半段正常、该命中的数字都命中了。**规则分这把尺子照不出退化**，只看分数会以为一切正常。

**结论**：采纳。准确度无劣化，且消除了两类线上故障。

`presence_penalty` 保留 0.4：它是**一次性**惩罚（token 出现过就固定扣分，不随次数累加），不会雪崩。

**落地**：

- SSOT [assistant.defaults.ts](../apps/server/src/common/defaults/assistant.defaults.ts) `DEFAULT_ASSISTANT_FREQUENCY_PENALTY = 0`，附单变量对照数据注释
- 回归守护测试 [assistant.defaults.spec.ts](../apps/server/src/common/defaults/assistant.defaults.spec.ts)
- commit `b2fd250`
- 线上 14 个助手已全部改为 0，改前备份表 `dialog_bak_20260901`（16 行）

---

## 2026-08-31 top_n 10 → 30 并启用 rerank

> 本条数据来自 [assistant.defaults.ts](../apps/server/src/common/defaults/assistant.defaults.ts) 中该常量上方的实测记录，非本次重跑。

**变量**：`top_n` 与 `rerank_id`，两个维度交叉对照。

**口径**：0820 语料 / 32 题 / `chunk_token_num=512` / **LLM judge 同一口径**（⚠️ 与上一条的规则分**不是同一把尺子**，两者的数字不能相互比较）。

**结果**：

| 配置                    |     分数 |
| ----------------------- | -------: |
| `top_n=10`              |     62.0 |
| `top_n=20`              |     68.5 |
| `top_n=30`              |     71.7 |
| `top_n=10` + rerank     |     70.9 |
| **`top_n=30` + rerank** | **82.3** |

rerank 与 top_n 改善的是不同的题、可叠加：rerank 提 top-k 内的精度（简答 68.8→91.2、检索边界 65.0→91.2），top_n 提召回覆盖面（跨项目 5.0→38.3）。两者同开时 5 个题型全部改善，无此消彼长。

**结论**：采纳 `top_n=30` + `BAAI/bge-reranker-v2-m3`。

**落地**：`DEFAULT_ASSISTANT_TOP_N = 30`、`DEFAULT_ASSISTANT_RERANK_ID`。注意 rerank 常量**不是兜底值**——实例没挂该模型时 `resolveDefaultRerankId()` 会退到任一可用 rerank 模型，再没有就不启用。

---

## 更早的优化

2026-05-09 之前的完整链路（平台修复 → 数据完整性 → 评估体系 → 检索/切片/Prompt 调优 → 默认值固化）见 [kb-optimization-report.md](kb-optimization-report.md)。其中影响最大的几项已固化进代码：

| 项 | 位置 |
| --- | --- |
| 923 字领域 prompt（列举完整 / 数字精确 / 试验段 vs 全工区 / 日期三分类） | `DEFAULT_ASSISTANT_SYSTEM_PROMPT` |
| `chunk_token_num=512` + DeepDOC + delimiter `\n` | [knowledge-base.defaults.ts](../apps/server/src/common/defaults/knowledge-base.defaults.ts) |
| 领域字典 → `important_keywords`（单独贡献 +7.0pp） | `scripts/eval/chunk-tagger.ts` |
| `max_tokens=8192` | `DEFAULT_ASSISTANT_MAX_TOKENS`（⚠️ 见下方待办：这个值在 OpenAI-API-Compatible 通道下会被 RAGFlow 丢弃） |

## 2026-09-03 失分归因诊断（未改动任何配置）

**动机**：回答"准确度还能不能再优化"。在动参数之前，先搞清楚现在的分丢在哪。

**方法**：对 5 道低分题（Q9 / Q12 / Q24 / Q27 / Q32）回放 top-30 检索证据，逐个 `mustContain` 数字判定三态——**召回里有没有 / 答案里有没有**；对召回里没有的数字，再直接以该数字为 query 反查库里到底存不存在。这一步能把"检索没捞到"和"原文就没有"彻底分开。

**结果**：

| 题 | 规则分 | 必答数字 | 召回里有 | 答案里有 | 真实性质 |
| --- | --: | --: | --: | --: | --- |
| Q9 | 0.67 | 3 | 2 | 2 | 数字口径分歧，待人工核对原文 |
| Q12 | 0.47 | 15 | 8 | 7 | **题目与标准答案错配** |
| Q24 | judge | — | — | — | **检索边界题，本就无正确文档** |
| Q27 | judge | — | — | — | **检索边界题，本就无正确文档** |
| Q32 | 0.00 | 3 | 2 | 0 | **生成漏答**（多问句只答了前两问） |

逐题结论：

- **Q24 / Q27 是"检索边界"题**，参考答案就是"知识库未涉及、无明确答案"，`reference.doc` 为空 —— hit 判定必然 `rank=0`。**这两题拉低的是度量，不是能力**。剔除后，30 道有正确文档的题**全部 rank=1，hit@1 = 100%**（而不是报表上的 93.8%）。它们真正要考的是"模型会不会老实拒答"，只有 judge 能评。
- **Q12 不是检索能力问题**。缺的 7 个数字全都在库里，其中 `151061 / 113400 / 37661 / 5025 / 3.32` 还挤在同一个 chunk。没被召回是因为问题问"资料品质方面哪些指标完成得好"，而那个 chunk 讲的是"测量资料物理点总数"，语义离得远。模型答了一级品率、空炮率（切题），标准答案却要求物理点统计（不切题）。**该修的是题集，不是检索参数**。
- **Q32 是真的生成漏答**：三个桩号里有两个已经召回到了，答案一个都没写。问题是多问句（问了组合方式、推荐方案、以及在哪几个桩号铺设），模型答完前两问就收尾了。
- **Q26 的 0.45 是打分假阴性**（详见 2026-09-01 条）：答案数字一个不差，只因用表格排版 `| 24 |`、而规则要求数字紧跟单位 `24个`。

**总结论**：**继续调检索参数（top_n / 权重 / 阈值 / 切片）的收益已经很小** —— 有正确文档的题已经 100% 召回到位。剩下的失分集中在**度量口径、打分规则、题集质量、评估缺口**四处，只有 Q32 一题是模型自身该背的锅。

---

## 2026-09-03 修评测口径 + 补 LLM judge + 修表格排版假阴性

**动机**：执行上一条诊断给出的 A / B / C 三项。都属于**度量层**改进——不改变系统行为，只让指标反映真实水平。

**变量**：三处，互相独立：

1. `RetrievalScore` 新增 `applicable` 字段。参考文档为空的题标记为"无从评判召回"，汇总时排除出 hit@1 / hit@3 / MRR 的分母。输出会显式打印排除了几题，不偷偷改数字。
2. 新建 judge 助手 `eval-judge`（`dbedfd6c…`）：不挂知识库、`temperature=0.01`、两个 penalty 都关；id 写入 `.env.eval` 的 `RAGFLOW_JUDGE_ASSISTANT_ID`。
3. 新增 `inlineTableUnits()`：把 Markdown 表头括号里的单位补给同列的**纯数字**单元格，接在 `cleanText` 之后。

第 3 项**没有**采用"无单位就放过"的简单做法 —— `unitsCompatible('炮','')` 返回 `false` 是刻意为之，注释写明是为了修 `顺8井北` 里的 `8` 误命中 `8m` 的旧 bug。只在表格结构内补单位，边界清晰、不放宽正文匹配。

**结果**：

先用两组真实答案离线重打分，确认只修该修的、不误伤：

|                            |   旧 |       新 |
| -------------------------- | ---: | -------: |
| Q26（fp=0，表格排版）      | 0.45 | **0.91** |
| Q26（fp=0.7，bullet 排版） | 0.91 |     0.91 |
| Q9 / Q12 / Q32             |    — | 完全不变 |

全量重打分：fp=0 组 `87.9% → 90.2%`；fp=0.7 组 `84.4% → 84.4%`（该组本就没用表格）。

**这翻转了 2026-09-01 那条的一个次要结论**：当时记录"fp=0 规则分低 0.016"，实为打分 bug 造成的假象。同口径（20 题）重打分后 **fp=0 反而高 5.8pp**（90.2% vs 84.4%）。fp=0 的采纳决定不变，依据更强了。

带 judge 的完整基线（32 题，口径见 2026-09-01 条，唯一区别是打分逻辑已含本条三项改动）：

| 指标          |       改口径前 |                   改口径后 |
| ------------- | -------------: | -------------------------: |
| MRR           |          0.938 |                  **1.000** |
| hit@1 / hit@3 |  93.8% / 93.8% |            **100% / 100%** |
| 检索统计题数  |             32 | 30（2 题无参考文档已排除） |
| answer-avg    | 87.9%（20 题） |     **86.4%（32 题全评）** |
| judge-pending |             12 |                      **0** |

⚠️ `answer-avg` 前后**不可比**：改口径前只统计 20 道规则题，改口径后 12 道 judge 题也进来了（它们平均分更低，拉低了总均值）。**86.4% 才是第一个覆盖全部 32 题的数字**，往后以它为基线。

与历史 `82.3` 同样**不能直接比**：那轮的打分细节已不可考，且当时是 `fp=0.7`、打分逻辑也没有本条的三项修正。

**结论**：三项全部采纳。检索侧至此确认无优化空间（有正确文档的 30 题全部 rank=1）。

**judge 首次开出的分**，暴露了规则分完全照不出的问题：

| 题 | judge | 性质 |
| --- | --: | --- |
| Q24 | **0.10** | 该拒答却过度作答：拿"整体里程碑计划 2025年9-12月"硬凑成工期规划 |
| Q13 | **0.37** | 列举不完整：参考答案要的"四级质量检查制度"**就在 top-30 第 7 位**，召回到了却没写进答案 |
| Q18 | 0.70 | — |
| Q7 | 0.80 | — |
| Q23 | 0.90 | — |
| 其余 7 题 | 1.00 | 含 Q27（同为拒答题，干净地答了"知识库未给出，无法回答"） |

Q24 与 Q27 是同类拒答题却一个 0.10 一个 1.00 —— **模型有拒答能力，但不稳定**。

**落地**：`scoring.ts`（`applicable` + `inlineTableUnits`）、`run.ts`（汇总分母）、`scoring.spec.ts`（+7 条测试，全套 119 条通过）、`.env.eval`（judge id）。

---

## 2026-09-03 系统提示词补三条规则（923 → 1447 字，v6）

**动机**：补上 judge 后，剩余失分几乎全落在生成侧，且三道低分题指向三条互相独立的缺陷（见上一条诊断）。

**变量**：`DEFAULT_ASSISTANT_SYSTEM_PROMPT`。检索参数、模型、知识库全部不动。经三轮迭代定稿：

| 版本 | 改了什么 |
| --- | --- |
| v4 | ① 规则 1 补「收尾前回扫，别漏成体系的大项」② 规则 5 补拒答判据「沾边内容按未给出处理」③ 新增规则 6「多问句逐条作答」 |
| v5 | 规则 5 再补「区分知识库*说了没有* vs _压根没说_」 |
| **v6** | 规则 6 补「问题前提与知识库相悖时，先一句话点破前提，不要埋进逐条列表」 |

**口径**：与 2026-09-01 条相同，唯一区别是打分逻辑已含 09-03 的三项修正。**每个版本跑两轮**——单轮均值波动约 ±1.1pp，一轮不足以下结论。

**结果**（32 题均值，v4/v5/v6 各两轮）：

| 版本 | 均值 | 各轮 | 稳定改善 | 稳定劣化 | 净 |
| --- | --: | --- | --- | --- | --: |
| 基线 | 0.864 | 0.864 | — | — | — |
| v4 | 0.890 | 0.896 / 0.885 | Q24 +0.80、Q13 +0.13 | Q16 −0.10 | +2.6pp |
| v5 | 0.894 | 0.883 / 0.906 | Q24 +0.80、Q18 +0.20、Q13 +0.10、Q30 +0.17 | Q16 −0.20 | +3.3pp |
| **v6** | **0.897** | 0.895 / 0.898 | Q24 +0.80、Q18 +0.13、Q13 +0.03 | **无** | **+3.0pp** |

「稳定改善 / 劣化」的判据是**两轮都相对基线同向变动**，单轮出现的差异一律按噪声处理。v4 曾显示 Q18 +0.10、Q26 +0.09，第二轮即掉回，属噪声。

**Q16 是这轮的主要教训**，全历程：`基线 1.00 → v4 0.90/0.73 → v5 0.60/0.80 → v6 1.00/1.00`。

该题问"哪些障碍区使用了可控震源"，而参考答案是"本项目中未曾使用可控震源"—— **问题的前提本身不成立**。基线答得干脆（"并未使用可控震源，全区采用炸药"）。v4 的「多问句逐条作答」规则把模型带偏，强制它用"1. 是否使用了：知识库未给出…"的格式开场，把结论埋进列表、还捎带塞进大段无关的 2009 年河山项目细节。

v5 的修补方向错了 —— 去补规则 5（拒答判据），结果 Q16 更差。看答案才发现内容一直是对的，问题出在**结论的位置和措辞**，根因在规则 6 而非规则 5。v6 改对地方后一次修复。

**结论**：采纳 v6。它是三个版本里均值最高（0.897）、唯一零劣化、且两轮波动最小（0.003，v5 是 0.023）的。v5 净值虽标称更高，但那是拿 Q16 的稳定劣化换来的。

**没有做的事**：Q32 三个版本都是 0.00。多问句规则确实生效了（v6 确实逐条编号作答），但 `mustContain` 要三个桩号，模型答的是道数。这题更接近标准答案偏严——问"对比了哪几种组合方式"，答"口/吕/品三种"本身切题。**不再为单题加 prompt 规则**，那是对着测试集过拟合。

**落地**：`DEFAULT_ASSISTANT_SYSTEM_PROMPT`（923 → 1447 字）。⚠️ **只影响新建助手**；线上 14 个既有助手的 prompt 仍是 923 字旧版，需另行同步。

---

## 2026-09-06 首次跑通 82 题全量：82.4%

**动机**：此前所有评测都只跑单批题集，"82 题"从没合并跑过。线上那个「全部82问题测试」助手就是为全量建的，但它那次会话问到第 33 题就撞上 `frequency_penalty` 乱码停了——也就是本文档第一条记录的那个故障。

**"82 题"是什么**：0420(20) + 0520(30) + 0820(32) 三批之和，仓库里没有单独的 82 题文件。已合并为 `dataset/questions-all82.json`，id 加批次偏移（0420 原号 / 0520 +100 / 0820 +200），`originalId` 保留原号。

**口径**：知识库「全部测试文件82问题」（26 文档），助手 prompt=v6、`top_n=30`+rerank、`fp=0`，chat `qwen3.8-max`，embedding `qwen3.7-text-embedding`，rerank `bge-reranker-v2-m3`。34 道 judge 题全部打分。

**结果**：**82.4%**。0420 76.1% / 0520 78.7% / 0820 89.9%。检索 MRR 1.000、hit@1 100%（80 题可评）。

0820 明显高于另外两批——它是 prompt 调优时做 A/B 用的批次，有"主场优势"。

**过程中修掉的一个评测缺陷**：`normalizeDocName` 只处理去后缀 / 去空格 / 转小写，**不管括号、顿号、下划线**。同一份文档导入不同知识库时文件名转换方式不同（题集写 `梁北二井（12采区）…`，库里是 `梁北二井_12采区_….md`），导致 82 题里 **24 题被误判成"没召回到"**。0820 那 32 题一直没暴露，是因为它跑在自己的库上、文件名同批导入。修复后可匹配数 58 → 73，检索指标才显出真实的 100%。剩 7 题是题集把文档名写得与实际不符（"2014**年度**"vs"2014年"、少了"**资料**"二字），属数据问题，未擅自改题集。

---

## 2026-09-06 4 个 pdf 从 Plain Text 重解析为 DeepDOC：82.4% → 85.5%

**动机**：82 题库的 `layout_recognize` 是 `Plain Text`，而所有调优都基于 `DeepDOC`（[knowledge-base.defaults.ts](../apps/server/src/common/defaults/knowledge-base.defaults.ts) 的 SSOT），口径不一致。

**范围只有 4 个文档，不是 26 个**：`layout_recognize` 只作用于 pdf 分支（[naive.py:1208](../ragflow-src/rag/app/naive.py#L1208) 的 markdown 分支只读 `chunk_token_num` 和 `delimiter`）。26 个文档里 22 个是 `.md`，DeepDOC 与 Plain Text 的产物完全一样。真正受影响的只有 4 个 pdf（共 425 页），涉及 9 道题。

**变量**：这 4 个 pdf 的 `layout_recognize` `Plain Text → DeepDOC` 并重新解析。库级配置一并改为 DeepDOC。其余全部不动。

**结果**（DeepDOC 跑两轮取均值，与单轮基线对比）：

|                    | Plain Text |                       DeepDOC |
| ------------------ | ---------: | ----------------------------: |
| 82 题总分          |      82.4% | **85.5%**（两轮 85.3 / 85.7） |
| **那 9 道 pdf 题** |      61.4% |          **80.8%（+19.4pp）** |
| 其余 73 题         |      85.0% |               86.1%（+1.1pp） |

9 题中 **4 题稳定改善、1 题稳定劣化**（判据：两轮都相对基线同向）：

| 题   | 基线 | DeepDOC 两轮        |
| ---- | ---: | ------------------- |
| Q119 | 0.00 | **1.00 / 1.00**     |
| Q9   | 0.50 | **1.00 / 1.00**     |
| Q104 | 0.60 | 0.90 / 0.90         |
| Q8   | 0.92 | 1.00 / 1.00         |
| Q113 | 0.90 | 0.70 / 0.83（劣化） |

**噪声地板**：其余 73 题两轮之间抖动 13 题、平均 0.08、最大 0.33。Q119(+1.00) 和 Q9(+0.50) 两轮零偏差，远超噪声，可信。而 +1.1pp 的"其余 73 题提升"落在噪声内，不作数。

chunk 数 697 → 619（三个 pdf 变少、一个略增）。这符合机制：Plain Text 按文本流硬切，跨栏跨页内容碎成多块；DeepDOC 先识别版面块再切，块更少但更完整。

**结论**：DeepDOC 对 pdf 值得，且效果精准落在 pdf 题上，没有溢出。**代价是 425 页在 CPU-only 机器上解析了 1 小时 20 分**（DeepDOC 每页要跑版面分析 + OCR + 表格结构识别三个模型，约 10 秒/页；Plain Text 是毫秒级）。以后要常态化入 pdf，需考虑 GPU（本机有 H100，RAGFlow 自带 `gpu` profile）或走 pdf→md 转换。

**运维教训**：ragflow 主机是**抢占式实例**，解析途中被回收过一次，"resource sold out" 是重新创建时抢不到同规格资源。RAGFlow 重启后自动接着解析，没丢数据。**长任务（入库、评测）必须能断点续跑**。

---

## 2026-09-06 prompt v7：把「每个数值必须带单位」提为独立要求 —— 85.7% → 87.7%

**动机**：82 题诊断发现 Q5、Q10 是最冤的失分 —— 数值全部答对，只因没写单位被判 0.30 / 0.00。prompt 规则 2 原本就写了"包含正负号和单位"，但这句埋在句末从句里，模型分条列举时几乎必省。

**变量**：只改规则 2。把"带单位"提进小标题并给三个反例：

> 2. **数字必须精确严格，每个数值都要带单位**：…**单位一律不得省略**——哪怕同一段里前文已经出现过该单位、哪怕单位在专业上不言自明、哪怕是分条列举，每个数值仍要各自带上单位。例如闭合差写"0.059m"而不是"0.059"，井深写"8-12m"而不是"8-12"，覆盖次数写"900次"而不是"900"。**漏写单位等同于答错。**

反例取的是这批语料的常见量（闭合差、井深、覆盖次数），不是把题目答案抄进 prompt。

**结果**（各两轮，判据仍是"两轮都站得住"）：

|        | 两轮        |      均值 |
| ------ | ----------- | --------: |
| v6     | 85.5 / 85.9 |     85.7% |
| **v7** | 87.2 / 88.1 | **87.7%** |

稳定改善：**Q10 `0.00 → 1.00`**、**Q5 `0.30 → 1.00`**（均两轮满分）、Q19 `+0.13`。

**顺带修掉一个打分缺口**：v7 一度让 Q12 从 1.00 掉到 0.86 —— 模型把"炮点总数 60300**个**"改写成"60300**炮**"、"接收点总数 236232**个**"改写成"236232**点**"，用词其实更专业。`UNIT_GROUPS` 里有「炮 ↔ 个」映射却**没有「点 ↔ 个」**，于是"点"被判未命中。补上映射（同时把「点」加进数字-单位正则）后，Q12 回到满分。这属于打分规则的缺口，不该由模型背。

**已知残留**：Q214 在 v7 某一轮把设备回收率 `99.99%` 写成了 `99.85%`（与数据完整率串了），另一轮正常，未达稳定劣化判据，按波动处理。若后续复现需单独查。

**落地**：`DEFAULT_ASSISTANT_SYSTEM_PROMPT`（1447 → 1598 字）、`scoring.ts` 的 `UNIT_GROUPS` + `NUMBER_TOKEN_RE`。⚠️ prompt 只影响**新建**助手，线上既有助手需另行同步。

---

## 2026-09-06 ⭐ 发现真正的召回天花板：`rerank_candidates_count = 64`

**这是本轮最重要的发现，且更正了此前两条结论。**

**背景**：82 题里 Q19 / Q213 / Q116 三题的参考内容压根不在 top-30 召回里，而它们的文档级 rank 全是 1。

**链路真相**：

```
混合检索出 top_k = 1024 个候选
        ↓
rerank 只对前 rerank_candidates_count = 64 个重排   ← 真正的天花板
        ↓
取 top_n = 30 交给模型
```

`rerank_candidates_count` 是 RAGFlow 的**静默默认值 64**（`db_models.py:1478`），UI 上不显眼，我们四个助手全是默认值。**`top_n=30` 实际是从 64 个里挑 30 个，真正的召回上限是 64，不是 1024。**

**实证**：把候选数放大到 400 后重新检索，那三题的参考内容排名是：

| 题   | 参考内容          | 放大后排名 |
| ---- | ----------------- | ---------: |
| Q116 | 地表一致性反褶积  |      **2** |
| Q213 | 三动态            |      **7** |
| Q213 | 四级质量          |      **9** |
| Q19  | 395 / 3400 / 5700 |     **17** |
| Q116 | 自动剩余静校正    |         19 |

**全在 top-30 以内**。说明 rerank 模型本身很好 —— 一旦让它看到这些段落，立刻排进前 20；问题是它**根本看不到**：这些段落在初检索（向量+关键词）里排在 65 名之后，连被评估的机会都没有。

**为什么以前没暴露**：Q213 在 0820 那个 **10 文档**的小库上，`四级质量` 能排到 top-30 第 7 位；换到 **26 文档**的大库就被挤出 64 名。**语料库一变大，这个默认值就成了隐形天花板。**

**因此更正两条旧结论**：

1. ~~"检索侧已无优化空间，hit@1=100%"~~ —— 那个 100% 是**文档级**的，只比对文件名，管不着召回的段落对不对。
2. ~~"下一步在大库上重测 `top_n`（30/50/80）"~~ —— **方向错了**。`top_n` 调到 80 也没用，池子里总共只有 64 个。**该调的是 `rerank_candidates_count`。**

**待做实验**：`rerank_candidates_count` `64 → 256`，预期 Q19 / Q213 / Q116 三题共约 2.4 分（3pp）可回收。`top_n=30` 保持不变，所以**喂给大模型的上下文与推理成本不变**，多出的开销只在 rerank 侧（bge-reranker 走 SiliconFlow 按量计费）。

---

## 2026-09-06 `rerank_candidates_count` 64 → 128：87.3% → 89.0%

**动机**：上一条发现的召回天花板。参考段落在初检索里排 65 名之后就进不了 rerank，调 `top_n` 无用。

**变量**：只改 `rerank_candidates_count`。prompt v7、`top_n=30`、DeepDOC 库、模型全不动。

**为什么是 128 而不是 256**：先试 256，**SiliconFlow rerank 返回 429（限流）**——候选量翻 4 倍叠加评测并发 3，超了速率上限。实测三道目标题的参考段落放大候选池后排名都在**前 20**，128 足够。同时给评测加了 `EVAL_CONCURRENCY` 环境变量（默认仍 3），这轮用 2。

**结果**（各两轮）：

|              | 两轮        |      均值 |
| ------------ | ----------- | --------: |
| 候选 64      | 86.9 / 87.7 |     87.3% |
| **候选 128** | 89.2 / 88.8 | **89.0%** |

目标三题只中两个，却有意外收获：

| 题 | 64 | 128 |  |
| --- | --- | --- | --- |
| Q115 | 0.00 / 0.00 | **1.00 / 1.00** | ⭐ 意外：那道"井深 8-12m 在召回里却没写"的题，原来也是候选池受害者 |
| Q116 | 0.50 / 0.43 | 0.73 / 0.80 | ✅ |
| Q213 | 0.30 / 0.30 | 0.40 / 0.40 | 有起色但仍低 |
| Q19 | 0.40 / 0.40 | 0.40 / 0.40 | ❌ 没动，说明它的问题不止候选池 |

**代价**：稳定劣化 4 题（Q104 −0.1、Q106 −0.17、Q207 −0.03、**Q210 −0.5**）。Q210 查过：候选池变大后 rerank 重新洗牌，含"380个"的段落被挤出 top-30，从 2/2 命中掉到 1/2。**扩大候选池是双刃剑**——捞回一批、也挤掉一批，净 +1.7pp。

**顺带修掉一个会让实验白做的坑**：`syncAssistantConfig` 的 PUT 是全量替换，body 里从来没有 `rerank_candidates_count`，**助手上设的值一开跑就被打回默认 64**。现已带上该字段、支持从 config 指定，并打进同步日志。

**落地**：`DEFAULT_ASSISTANT_RERANK_CANDIDATES_COUNT = 128` + `assistant.service` 建助手时传入。⚠️ 只影响新建助手。

**未验证**：256 及以上。要试需先解决 rerank 限流（降并发或换 rerank 供应商）。

---

## 2026-09-06 prompt 压缩重写（1598 → 1122 字）：不采纳，差异在噪声里

**动机**：prompt 从 923 字一路加到 1598 字，怀疑有冗余。做语义不变的压缩重写，看能否白省 30%。

**变量**：只改措辞，不动语义。砍掉逐轮追加攒下的排比（"哪怕…哪怕…哪怕"→"…都不是省略的理由"），日期段三段定义+三条规则压成两段，反例保留。压缩后校验 8 个已验证语义点全在。

**结果**（各两轮）：

|               | 两轮        |  均值 |
| ------------- | ----------- | ----: |
| v7（1598 字） | 89.2 / 88.8 | 89.0% |
| v8（1122 字） | 87.4 / 88.4 | 87.9% |

表面差 1.1pp，但**两轮都站得住的差异只有 4 题、净 −0.19 分（≈ −0.2pp）**：v8 更好 1 题（Q130 +0.04），更差 3 题（Q116 −0.03、Q224 −0.03、Q230 −0.17）。其余 0.9pp 全在噪声里。

**过程中的一个误判值得记**：v8 第一轮 Q118 从 1.00 掉到 0.00，查下来是"野外施工难点"只列了 6 条、v7 列 8 条，漏掉的正好含唯一必答项"落差 3m"。当时判断是压缩削弱了"列举完整"规则的约束力 —— **但第二轮 Q118 回到 1.00，是噪声**。单轮证据加上看似合理的机制解释，很容易编出一个错误的因果故事。

**结论：不采纳。** 不是因为压缩有害，而是**测量精度不够**：单轮波动 ±1.1pp，要检验的差异约 0.2pp，两轮分辨不出来，要 4-6 轮才够（3 小时以上）。而收益仅约 300 token（占请求总上下文约 3%），不值得为一个测不准的差异冒险。

**更值得做的减负方向**：消融规则 3「区分试验段 vs 全工区」和规则 4「多文档汇总」（约 200 字，2026-05 加入，从未单变量验证）。删/留是明确的二选一，掉不掉分看得出来；而"同义换说法"这类改动天生落在噪声里。

---

## 2026-09-06 换 embedding：BAAI/bge-m3 —— 84.5%，不采纳

**动机不是提分，是可部署性**。当前冠军配置用的 `qwen3.7-text-embedding` 走阿里百炼 API，**内网私有化环境根本调不到**。也就是说 89.0% 这个数字在生产上够不着，必须在「能自己部署的模型」里重新找上限。

**变量**：建平行库 `全部测试文件82问题-bgem3`，切片配置（DeepDOC / chunk_token_num 512 / delimiter `\n`）逐项照抄，助手参数（v7 prompt / top_n=30 / rerank_candidates_count=128 / 同一个 rerank 模型）全部复制，**只换 embedding**。

**结果**（各两轮）：

|                                   | 82 题  |    差 |
| --------------------------------- | ------ | ----: |
| qwen3.7-text-embedding（API only）| 89.0%  |     — |
| BAAI/bge-m3（可私有化）           | 84.5%  | −4.5pp |

**检索级的解释**（不是"分低"这么笼统）：拿 Q115 逐条比对两库的召回，**top-3 完全相同**，分歧从第 4 条开始 —— qwen 返回含 `井深8–12m` 的参数表格，bge-m3 返回讲"分析方法"的正文段落。两者都与问题语义相关，但我们的评分标准要求报出准确数字。**qwen 更倾向锚定含数值的段落，bge-m3 更倾向语义贴合**，在这套评分口径下后者吃亏。

**一个必须澄清的误解**：bge-m3 的卖点之一是同时输出 dense / sparse(lexical) / ColBERT 三种表示，容易让人以为"它自带 BM25 所以关键词更强"。**RAGFlow 只消费 dense 向量**，关键词那一路来自 Elasticsearch 自己的 BM25，靠 `vector_similarity_weight` 融合（`search.py:34`）—— 换成 bge-m3 并不会让关键词检索变强，那部分链路根本没变。

**口径差异**：bge-m3 库 3796 chunk，原库 3824，差 28 个 chunk 来自 2 个 pdf 的 DeepDOC 版面识别非确定性，非模型差异。

---

## 2026-09-06 换 embedding：Qwen3-Embedding-4B —— 85.7%，可私有化路线的当前最优

**同一个动机的延续**：`qwen3.7-text-embedding` 是阿里百炼 API-only，内网调不到，89.0% 在生产上够不着。bge-m3 试过了（84.5%），这次试 Qwen 官方开源的 `Qwen3-Embedding-4B`（SiliconFlow 上跑，权重开源，内网可用 Xinference/vLLM 自部署）。

选 4B 而非 8B/0.6B：向量维度 8B=4096 / 4B=2560 / 0.6B=1024，4B 在效果与显存/存储开销之间。

**变量**：同样建平行库，切片配置与助手参数逐项照抄，只换 embedding。库 chunk 数 **3796，与 bge-m3 完全一致**，两者严格同口径。

**结果**（各两轮）：

| embedding                          | 轮1   | 轮2   |  均值 | 可私有化 |
| ---------------------------------- | ----- | ----- | ----: | -------- |
| qwen3.7-text-embedding             | 89.2% | 88.8% | 89.0% | ❌ API only |
| BAAI/bge-m3                        | 85.1% | 83.8% | 84.5% | ✅ |
| **Qwen/Qwen3-Embedding-4B**        | 85.2% | 86.2% | **85.7%** | ✅ |

**逐题看两轮都站得住的差异**（对 bge-m3）：赢 3 题 Q128 / Q209 / Q210（+1.77 分），输 2 题 Q110 / Q116（−0.40 分），净 **+1.37 分 ≈ +1.7pp**。差异真实，不是噪声。

### 入库时踩的坑：注册模型时 `max_tokens` 填错，会冤枉模型

首次解析 **26 个文档挂了 1 个**，SiliconFlow 返回 `400 {"code":20015,"message":"The parameter is invalid"}`，且**重试必然复现**（不是限流的 429）。

根因是注册这个模型时 `tenant_model.extra.max_tokens` 填了 **33000**（bge-m3 和 qwen3.7 都是 8192）。RAGFlow 按 `truncate(t, embedding_model.max_length - 10)` 截断（`rag/svr/task_executor_refactor/embedding_service.py:95`），填 33000 等于不截断，那个 682KB md 文件里的超长表格 chunk 原样发出去就被拒。改回 8192 后重解析通过，chunk 数从 3560 补齐到 3796。

**教训**：`max_tokens` 填的是**服务商对该模型的实际输入上限**，不是模型架构的上下文长度。填大了不会报"配置错误"，而是在入库到某个超长 chunk 时才炸，表现为"某个文档解析失败"——很容易误判成模型能力问题。RAGFlow 0.27 起这个配置在 `tenant_model` 表（旧的 `tenant_llm` 已空）。

### 与 qwen3.7 的 3.3pp 差距落在哪

|      | qwen3.7 | bge-m3 | 4B   |
| ---- | ------- | ------ | ---- |
| Q6   | 0.85    | 0.38   | 0.45 |
| Q115 | 1.00    | 0.00   | 0.00 |
| Q227 | 1.00    | 0.50   | 0.60 |

**两个可私有化模型输的是同一批题**，失败模式一致：召回了语义相关的正文段落，没召回含具体数字的表格（Q115 要 `井深8-12m`，两者都返回了讲"分析方法"的正文）。

这**推翻了本文档「不建议做的」里那条**「没有任何一例失分能归因到 `vector_similarity_weight`」—— 现在有三例，且指向明确。详见下方优化候选 I。

### 所有方案共同的地板：5 道题的评分规则丢了信息（当日已修）

Q101 / Q105 / Q232 在**三个 embedding 下全部 0.00**，Q19（0.40）和 Q212（0.47）三者也都上不去，合计让每个方案都丢掉约 4.1 分（≈5pp）。

**当时判断是「题目与标准答案错配、需业务拍板」，这个判断是错的** —— 翻业务原始题库后确认，是我们把标准答案转成评分规则时只抽了数字、丢了定性内容。已于同日修正，详见 H 节。**因此本条记录里的 85.7%（以及 89.0% / 84.5%）都是旧规则下的分数。**

---

## 2026-09-06 ⭐ 修好评分规则后重测：可私有化方案与 API 方案只差 0.9pp

**这是当前口径下的最终结论。** 修完 6 道题的评分规则（见候选 H）后，用同一把尺子重测：

| embedding                          | 分数            | 可私有化 |
| ---------------------------------- | --------------- | -------- |
| qwen3.7-text-embedding             | 91.6%（一轮）   | ❌ 百炼 API only |
| **Qwen/Qwen3-Embedding-4B**        | **90.7% / 90.7%**（两轮一致） | ✅ |
| 4B + `vector_similarity_weight`=0.5 | 90.1%（一轮）  | ✅（候选 I，不采纳） |

**差距从 3.3pp 缩到 0.9pp，其中 2.4pp 是测量偏差而非能力差异。** 旧规则把两边的 Q101/Q105/Q232 一起判零、把 Q115 的 4/5 个正确要点判零，掩盖了 4B 的真实水平。

**0.9pp 落在单轮波动（±1.1pp）之内，且 qwen3.7 只跑了一轮**，因此严格的表述是**两者基本持平**，不是"qwen3.7 略优"。要断言优劣需要 qwen3.7 再补一轮。

4B 两轮 90.7 / 90.7 完全一致，是本项目至今最稳的一组数。

**结论：内网私有化部署选 `Qwen/Qwen3-Embedding-4B`**，用 Xinference/vLLM 加载同一份开源权重。代价是入库速度明显低于 bge-m3（4B 参数、2560 维），全量文档入库时间要按这个比例重估；ES 的向量存储开销也随维度上升。

**剩余的真实差距**：Q19（4B 未召回到微测井解释那段）和 Q227。量级不足以支撑继续调检索参数 —— 候选 I 已验证无效。

---

## 2026-09-06 重写通用 judge rubric：中性但收益远低于预期（+0.4 分）

**动机**：失分归因发现 36 道 judge 题里有 27 道用同一个「通用评分」模板（`import-questions.ts` 自动生成），
满分率仅 9/27，而同批 mustContain 题是 38/46，怀疑 judge 缺锚点、系统性判低。

**改法**：把模板从「关键事实覆盖 0.6 / 准确性 0.3 / 完整性 0.1 + 一张 1.00/0.50/0.20-0.40 的粗档速查表」
改成可数的「命中要点数 ÷ 要点总数」，并写明：措辞不同不扣分（参考答案是文档摘录、不是标准措辞）、
答出参考答案之外的正确内容不扣分、参考答案只有一两个要点时全覆盖即 1.00。
硬要求保留：带数值的要点必须报出相同数值；矛盾 −0.2、编造 −0.3。
模板抽到 `scripts/eval/judge-rubric.ts`，`import-questions.ts` 与题集共用同一份。

**结果**（36 道 judge 题均分）：

|         | 旧模板 | 新模板 |     差 |
| ------- | -----: | -----: | -----: |
| 4B      |  0.851 |  0.862 | +0.011 |
| qwen3.7 |  0.907 |  0.916 | +0.009 |

**两个模型涨幅几乎相同（+0.011 / +0.009），模板是中性的**，不偏袒任何一方，可以留用；
但**收益远低于「挽回 1~2 分」的预期，实际只有 +0.41 分**。judge 题里 4 涨 3 降，
不是"系统性判低被纠正"，而是判分重新分布。

**一个必须记下的误判**：我拿 Q107 当"judge 判低"的证据（模型完整覆盖参考答案两个要点、还多给了细节，只得 0.80），
新模板下它反而**降到 0.56**（三次评分 0.47/0.47/0.73）。新模板让 judge 把参考答案拆得更细，
模型漏掉的"减少对地面设施的破坏"这半句被单独计成一个要点 —— **0.80 本来就是合理判分，是我误读了。**
教训：只看一道题就下"系统性"的判断，抽样量不够；带着结论去改，容易把噪声当信号。

**稳定性代价可忽略**：三次评分极差均值 0.017 → 0.026，完全一致的题数不变（30/36）。

---

## 2026-09-06 剩余失分的构成（4B，91.6%）

修完 6 题规则 + Q14/Q128 单位与二选一 bug + 新 rubric 后：

| | 分数 |
| --- | --- |
| qwen3.7（API only） | 92.1% |
| **Qwen3-Embedding-4B（可私有化）** | **91.6%** |

**差距 0.5pp，完全落在单轮波动内 —— 两者实质持平。**

失分归因（按占比）：

- **通用 rubric 的 judge 题**：曾占 48% 失分。改模板后确认其中既有误判也有真失分，不能一概而论
  （Q218 标准答案列 10 项技术、模型答 7 项，0.70 判得对）。
- **mustContain 规则残留问题**：已修 Q101/Q105/Q115/Q212/Q232/Q19 六题 + Q14（两种合法表述被当成都要答）
  + Q128（横纵比 0.63 的单位被错标成"炮"）。Q210 同类未改（问"哪些对策"却只查 410/380 两个数字）。
- **真实能力差距**：Q19（未召回微测井段）、Q115（未报出井深准数）、Q227（rank=0）、Q11（两模型都漏答"高斯6°分带"）。
  候选 I 已证明调 `vector_similarity_weight` 救不了这类题。

---

## 下一步优化候选

> A（检索指标口径）、B（补 LLM judge）、C（表格排版假阴性）已于 2026-09-03 完成；D（prompt v6）同日完成。以下基于 2026-09-06 对 82 题里 10 道低分题的逐题诊断。

### ⚠️ 先更正一条旧结论：hit@1=100% 是**文档级**的

之前记录过"检索侧已无优化空间"，**这是错的**。`scoreRetrieval` 只比对文件名，管不着召回的**段落**对不对。诊断发现 3 道题的参考内容压根不在 top-30 里，而它们的文档级 rank 全是 1：

- **Q19**（0.27）：参考的表层结构数值（`395` / `0-4m` / `3400` / `5700` / `1200-3000`）**一个都不在 top-30**。模型只好拿同一份文档里另一段表层数据作答，数值整套对不上。
- **Q213**（0.30）：`四级质量` / `三动态` 都不在召回里。**但在 0820 那个 10 文档的小库上，`四级质量` 排在 top-30 第 7 位** —— 换到 26 文档的大库就被挤出去了。
- **Q116**（0.40）：参考的 5 项技术只召回到 2 项。

**`top_n=30` 这个最优值是在 10 文档的小库上测出来的**，库规模翻倍后未必仍然合适。

### F. 在 26 文档的大库上重测 top_n（最高优先级）

Q213 是直接证据：正确 chunk 就在库里，只是排不进 top-30。测 `top_n` = 30 / 50 / 80，看 Q19/Q213/Q116 是否回升。代价小、假设明确。

注意 top_n 增大会让上下文变长、成本上升，且可能引入干扰——两轮判据照旧。

### G. 评测指标补 chunk 级命中判定

题集 `reference` 里本来就有 `section` 字段（如"参考文档：…章节"），可用它判断召回段落对不对，而不是只看文件名。**不补这个，chunk 级失败会一直被文档级 100% 的假象盖住**——这次要不是逐题翻检索证据，根本发现不了。

### H. ~~题集勘误 —— 需要业务同事拍板~~ → 是我们自己的评分规则丢了信息（2026-09-06 已修）

**先更正一个错判。** 本节原先的结论是「4 道题的题目与标准答案错配，必须业务同事拍板」。翻了业务出的原始题库
（`test-docs/*/RAG问题和参考答案*.docx`）之后确认：**业务出的题没有问题，是我们把标准答案转成机器评分规则时，
只抽了里面的数字，把定性内容整个丢了。**

| 题       | 业务给的标准答案                                                       | 我们的规则实际在查什么                        |
| -------- | ---------------------------------------------------------------------- | --------------------------------------------- |
| **Q101** | 采用**单井微测井、双井微测井、静水面测量**相结合的方法，共完成 521个/33个 | 只查 `521`、`33`                              |
| **Q105** | 3 个共同难点 + 3 条技术对策，原文写明**「答案不唯一，合理概括即可」**    | 只查一个 `900`                                |
| **Q232** | 铺设**口字型单串、吕字型两串、品字型3串**；推荐单串；低频拓宽 **2-3Hz**  | 只查三个铺设桩号                              |
| **Q212** | 一段指标罗列                                                            | 15 个数字**全部**要命中                       |
| **Q19**  | 只给了答案，未给评分规则                                                | rubric 是我们自己写的，判「出现别的数字就归零」 |

Q101 问「用哪些方法」，业务答案第一句就是三种方法，我们的规则里一个方法都没有；Q232 问「对比了哪几种组合方式」，
答案写得明明白白是口字型/吕字型/品字型，我们抽的是它们后面跟的桩号 —— 连问题里明问的「低频拓宽了多少 Hz」（2-3Hz）都没查。

**离线用已有答案重打分验证**（三个 embedding 表现一致，此处取 qwen3.7 一轮）：

| 题   | 旧规则 | 新规则 | 说明                                             |
| ---- | -----: | -----: | ------------------------------------------------ |
| Q101 |   0.00 |   0.75 | 三种方法模型**本来就全答对了**，只是规则没查     |
| Q232 |   0.00 |   1.00 | 三问全答对（组合方式 / 推荐单串 / 拓宽 2-3Hz）   |

**改法**（2026-09-06 已落地，四个题集文件同步）：

- **Q101**：补回三种方法并设 `critical`，521/33 保留为普通项 —— 问什么就主要查什么，数量作为完整性加分。
- **Q105**：改走 LLM judge。业务原文写明「答案不唯一，合理概括即可」，这类题规则匹配天生不适用。rubric 按 3 难点 + 3 对策计分，不因缺某个数字扣分。
- **Q212**：改走 LLM judge，按 4 类指标计分（测量物理点/复测率、一级品率、深层信噪比、覆盖次数），数字作为佐证，不再要求 15 个数字全中。
- **Q232**：换成三种组合方式 + 推荐单串 + 拓宽 2-3Hz；桩号是铺设位置、不在三问范围内，不再作为必答项。
- **Q19**：rubric 改为「B 项只看标准答案那四组数值是否命中；两组数据都给出且区分了来源属于更完整的回答，照常计分；只有完全没给本区微测井数值时才判 0」。

**只动了评分规则，题目、业务标准答案、参考文档一字未改**（已用脚本逐字段校验，且确认未误伤其余 77 题）。

#### 补第 6 道：Q115 —— 整份标准答案只查一个数字

修完上面 5 题后跑候选 I，发现 Q115 在 4B 上仍是 0.00。查下来它是同一个病，只是漏网了：

- 标准答案：「采用**单井激发、高爆速炸药**；**井深 8–12m、药量 2–3kg**，通过**井深与药量对比试验**最终确定。」——五个要点
- 旧规则：`mustContain` 只有一项，数字 `12m`

4B 答出了「单井激发」「炸药震源，高爆速炸药」「药量2-3kg」「根据试验确定」四个要点，只因没报出准确井深（它答的是"井深：根据试验确定"）就被判 **0.00**。

改为五个要点各算一项后：**qwen3.7 从 1.00 降到 0.80**（它漏了药量）、**4B 从 0.00 升到 0.60**。新规则对两边都更严，不是单方面放水 —— 而报不报得出准确井深仍然体现在分数上（0.80 vs 0.60），那是真实的召回差异。

**这次没有扩大到全量。** 扫描发现题集里有 39 道题的 `mustContain` 全是数字类型，其中一批标准答案是大段定性论述（Q211 的答案 827 个汉字却只查 1 个数字）。但逐一核对得分后确认：**Q211/Q124/Q222/Q123/Q219/Q205 两个模型都拿 1.00** —— 那个数字恰好是答案的关键锚点，答对内容自然会带上它。**规则脆弱，但实际只有 Q115 被打中。** 未被触发的题不动，避免为改而改；这个结构性脆弱记录在此，日后哪题异常归零可先照此排查。

#### 附带的一课：「只认字面」和「只认数字」是同一类错误

Q232 改完第一次验证只到 0.30 —— 规则写的是 `口字型`，而模型的实际输出是 `“口”字型单串铺设`，**引号夹在字中间**，
字面匹配直接漏判。改成正则 `口\s*["“”'‘’]{0,2}\s*字\s*[型形]` 后才拿到 1.00。

修评分规则时把「只抽数字」换成「只认字面」，等于换了个姿势犯同一个错。**凡是 `type: 'string'` 的 mustContain，
都要想一遍模型可能怎么加标点、换同义字。**

#### ⚠️ 口径变更：本文档此前所有分数都是旧规则下的

89.0% / 85.7% / 84.5% 这些数字都按旧规则算出。新规则下三者都会上涨。由于这 5 题在三个 embedding 上表现一致
（Q101/Q232 都是同样的 0 → 0.75/1.00），**相对排序预期不变**，但**跨这条线的绝对分数不可直接比**。

### I. ~~调 `vector_similarity_weight`~~ —— 已验证，不采纳（2026-09-06）

**原假设**：两个可私有化 embedding 在 Q6 / Q115 / Q227 上共同失分，失败模式都是「召回语义相关的正文、漏掉含数字的表格」，怀疑向量权重 0.7 偏高、BM25 该多担一点。

**做法**：4B 库上建独立助手 `ab-qwen4b-vw05-temp`，只改 `vector_similarity_weight` 0.7 → 0.5，其余逐项照抄。

**结果**：**90.1% vs 基线 90.7%（两轮均为 90.7），净 −0.6pp。**

目标三题一道都没救回来：Q19 0.40→0.40、Q115 0.00→0.00、Q227 0.50/0.60→0.70（在噪声内）。而副作用是实打实的：Q113 / Q213 / Q214 / Q226 / Q230 五题变差（−1.35 分），只有 Q125 / Q126 / Q218 三题变好（+0.67 分）。

**假设被证伪，且诊断本身有问题**：事后发现 Q115 的失分主要是评分规则只查一个数字（见 H 节），拿它当"检索短板"的证据本就不成立。**先修测量，再调参数** —— 否则会拿着错误的失分归因去调一个本来没问题的参数。

真实的召回差距只剩 Q19（4B 没召回到微测井那段）和 Q227，量级不足以支撑继续在这个方向上调参。

### 不建议做的

- ~~**继续加 `top_n` 之外的检索参数调整**（`vector_similarity_weight`、`similarity_threshold`）：目前没有任何一例失分能归因到它们。~~
  **2026-09-06 更正**：换 embedding 的对照实验推翻了这条。bge-m3 与 Qwen3-Embedding-4B 在 Q6 / Q115 / Q227 上共同失分，失败模式都是「召回语义相关的正文、漏掉含数字的表格」，这正是 `vector_similarity_weight` 管的事。见候选 I。
- **重新切片**：至今没有发现"数字被切片切散导致召回不到"的实例。
- **为单题加 prompt 规则**：Q232 三个 prompt 版本都是 0.00，已确认是标准答案偏严，再改就是对着测试集过拟合。

## 已知待办

- **`max_tokens` 在当前链路上是失效的**。RAGFlow `rag/llm/chat_model.py` 的 `_clean_conf()` 会 `del gen_conf["max_tokens"]`，白名单里只认 `max_completion_tokens`。也就是说我们设的 8192 从未生效，长度上限由上游默认值决定。
- **评测脚本的 `.env.eval` 里 `RAGFLOW_HOST` 仍是 `http://localhost:9380`**（历史遗留）。本机跑必须 inline 覆盖成 `http://ragflow:9380`；`package.json` 里的 `eval:*` script 已带该前缀，手敲 `tsx` 时要自己加。
